# Runtime dependencies — version pins mirror pyproject.toml [project.dependencies]
pandas>=1.3.0
matplotlib>=3.4.0
pillow>=8.0.0
openpyxl>=3.0.0
odfpy>=1.4.0
xlrd>=2.0.0
pyyaml>=5.4.0

# Development / testing extras (install with: pip install -r requirements.txt -r requirements-dev.txt)
# See pyproject.toml [project.optional-dependencies] for the full dev toolchain.
